# Auto-generated by RCCS — do not edit manually.
# Regenerate with: rccs generate --config <archi-config.yaml>
services:
  mirror:
    image: nginx:alpine
    container_name: rccs-mirror
    volumes:
      - ./mirror:/usr/share/nginx/html:ro
      - ./nginx/sites:/etc/nginx/conf.d:ro
    ports:
      - "8080:80"
    networks:
      restricted_mirror:
        aliases:
          - cms-talk.web.cern.test
          - twiki.cern.test

networks:
  restricted_mirror:
    name: restricted_mirror
    internal: true
