# Auto-generated by RCCS — do not edit manually.
# Shared across all mirrored sites.  Maps the request's query string
# ($args) to a filesystem-safe filename suffix using the same encoding
# as CacheRawDownloaderMiddleware._encode_query():
#
#     ?page=3            -> __q__page-3
#     ?page=3&order=asc  -> __q__page-3__order-asc
#
# Extend this map with extra regex entries for query shapes your sites
# use.  Anything not matched falls back to the plain cached file via
# try_files, so unmapped queries simply serve the no-query version.

map $args $rccs_qsuffix {
    default                                "";
    ""                                     "";

    # Single-key patterns (Discourse pagination, feeds, etc.)
    ~^page=([0-9]+)$                       __q__page-$1;
    ~^order=([A-Za-z0-9._-]+)$             __q__order-$1;

    # Common two-key combinations.
    ~^page=([0-9]+)&order=([A-Za-z0-9._-]+)$  __q__page-$1__order-$2;
}
