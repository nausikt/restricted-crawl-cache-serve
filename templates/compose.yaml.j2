# Auto-generated by RCCS — do not edit manually.
# Regenerate with: rccs generate --config <archi-config.yaml>
#
# Absolute volume paths are baked in at generate time so this file is
# self-contained and `podman compose up` / `docker compose up` works
# from any directory.
#
#   MIRROR_ROOT           -> {{ mirror_root }}
#   GENERATED_OUTPUT_DIR  -> {{ generated_output_dir }}
services:
  mirror:
    image: nginx:alpine
    container_name: rccs-mirror
    volumes:
      - {{ mirror_root }}:/usr/share/nginx/html:ro
      - {{ nginx_sites_dir }}:/etc/nginx/conf.d:ro
    ports:
      - "{{ mirror_port }}:80"
    networks:
      {{ network_name }}:
        aliases:
{%- for alias in aliases %}
          - {{ alias }}
{%- endfor %}

networks:
  {{ network_name }}:
    name: {{ network_name }}
    internal: true
